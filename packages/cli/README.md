# thearchy-cli

CLI package for 神治（Thearchy）, a deterministic local multi-agent quality governance system.

```bash
npx thearchy-cli doctor
npx thearchy-cli install --target all
```

The installed executable is `thearchy`.
